{
  "$schema": "http://json-schema.org/draft-07/schema",
  "version": 2,
  "title": "Zip executor",
  "description": "Create zip archive of extension app for publishing",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project where the component is located.",
      "$default": {
        "$source": "projectName"
      },
      "x-dropdown": "projects",
      "x-prompt": "What project contains the extension?",
      "x-priority": "important"
    },
    "outputFileName": {
      "type": "string",
      "description": "Name of the zip output file. Defaults to versioned archive name."
    }
  },
  "required": ["project", "outputFileName"]
}
