{
  "$schema": "http://json-schema.org/schema",
  "$id": "GbNxRefreshCli",
  "title": "Refresh generated CLI application assets.",
  "description": "Refresh generated CLI application assets.",
  "examples": [
    {
      "command": "nx g @gb-nx/cli:refresh --project myapp --ts --main",
      "description": "Generate the Typescript files and main.ts entrypoint for the myapp CLI application"
    }
  ],
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      },
      "alias": "p",
      "x-prompt": "What project would you like to target?"
    },
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false,
      "x-priority": "internal"
    },
    "all": {
      "type": "boolean",
      "description": "Specifies if all files should be generated"
    },
    "ts": {
      "type": "boolean",
      "description": "Specifies if command typescript definitions should be generated"
    },
    "main": {
      "type": "boolean",
      "description": "Specifies if build target main should be generated"
    },
    "markdown": {
      "type": "boolean",
      "description": "Specifies if commands.md should be generated"
    }
  },
  "additionalProperties": false,
  "required": []
}
